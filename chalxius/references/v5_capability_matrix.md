# V5 capability preservation matrix

The 1.0.4 Frontier Context Handoff Repair preserves the 1.0.3 coordination
surface and makes two existing operations compose: exact head-bound context
follows a unique `plan-round` successor, while true multi-successor ambiguity
remains unattached and concrete reattachment absorbs an unattached duplicate.
After context handoff, Main inspects the Fact frontier and carries `Fact window
clock: n/4`. Explicit host compaction alone increments it, a completed packager
window resets it, and `4/4` dispatches one one-or-two-package window to a
suitable free slot. User deferral preserves `4/4 deferred` until lifted. This
adds no daemon, automatic scheduler, selector, package effect, or truth effect.

The 1.0.3 Research Route Coordination Repair composes existing Research
surfaces into a reliable Main working memory and asynchronous Fact route. It
adds Campaign head context and sparse landmarks, packager-owned bounded route
selection without Campaign-head mutation, rigid unique-COW/split projection, scope-owned fixed supervision
guidance, prospective adverse-learning retirement, and reviewed split logical
relations. It adds no automatic semantic choice, scheduler, compatibility
layer, Research rewrite, package effect, verifier effect, Gateway
effect, or Fact effect.

Recent attainment is clocked by context compaction or natural curation, not by
a hard four-entry limit. Sparse landmarks have no numeric quota and grow with
the Research graph. The separate recent queue uses 64 only as a high-water
guard; routine summaries preview four recent items and eight landmarks while
retaining exact total counts, identity digests, and full diagnostic access
without silent first-N selection. `fact-frontier --target` requires
`--campaign`; route-needing landmarks sort before certified history, and the
256-item view preserves complete identity count/digest. Direct-Research plans
freeze selection mode and exact Main anchors; every sealed component contains
an anchor and connects extra unmarked nodes through actual package edges, while
existing-mark plans remain exempt. Supervisor
`ready`/`needs_split` is a strong mechanical recommendation only. Planning
returns `mechanical_proposal_ready` with `mechanical_package_proposal` plus
digest and does not seal; the packager seals once, may replace the interface or
choose whole-node/COW routing. A requested split pauses packaging for a Research
repair worker and ordinary proof/source supervision; no Fact-side split
supervisor exists. The verifier reviews every record/interface/edge, and Gateway
alone certifies. The historical fast lane is only bounded one-to-one
complete-node minor COW with full same-verifier component recheck; split or
structural ambiguity returns to ordinary Research.

All lower release overlays are frozen then-current history. Old Candidate
commands are procedurally reserved for explicitly selected 0.x completion or
audit; the runtime does not authenticate pre-1.0 provenance, and this matrix
adds no provenance or identity gate.

The 1.0.2 Capability Budget Audit Repair changes no command or role. It scopes
the existing 64 MiB bound to one structured Repair manifest while retaining a
command-local read-once byte and path/digest-conflict snapshot; all containment,
no-follow, hash, conflict, and authority checks remain unchanged.

The 1.0.1 Fact Alpha Field Repair overlay keeps certification a sparse property of exact
Research nodes. It adds Main importance marks, a derived Fact frontier,
content-addressed batch plans, component-isolated verifier decisions,
deterministic Gateway grants, COW-driven re-verification, and direct certified
Research premises for future Research/task cards. It removes the need for new
duplicate Fact prose, internal statement-surface retrofits, and mandatory
whole-Candidate adverse work on the prospective path. Legacy admitted Facts
remain readable and unmapped. Research remains freely schedulable; Fact
packaging has no timer, threshold, or automatic dispatch. Exact zero-state root
advice and scoped-filter counts assist Main without making choices. New clean
supervisors may emit `ready` interfaces or `needs_split`; all-ready plans seal
mechanically, while legacy/missing/custom work retains the packager fallback.
Research artifacts remain the sole evidence-locator layer, and the V5 verifier
role is restricted to its three independent decision commands.

The 0.9.18 Frontier Breadth Parity overlay carries up to sixteen
Main-selected heads per Campaign Target through legacy checkpoint reading,
current state validation, planning, reconciliation, and ordinary projection.
The Target window remains independently bounded, derived successor fan-out
retains count/digest diagnostics, and exact Research remains retrievable by id.
Larger claim budgets improve mathematical judgment without restoring repeated
workflow topology. No paging, automatic selection, scheduler, compatibility
layer, Candidate effect, or Fact effect is introduced.

The 0.9.17 Stepping-Stones Continuity overlay preserves every routine frontier
head and supervision scope while removing per-action copies of forensic route,
terminal, evidence, hash, empty-field, and synonymous-state detail. Concise
mathematics, current-route Research ids, next action, and exact actionable
Research/round ids remain visible; `--diagnostic` remains complete. The named
Main semantic **摸石头过河** saves and resumes the exact research boundary around
a material integrated repair/install interruption. It adds no automatic
trigger, scheduler, timer, installer, publication action, Candidate effect, or
Fact effect.

The 0.9.16 Bounded Research Coordination overlay subtracts exact completed or
in-flight scopes from default supervision planning, bounds routine nested
Campaign goal projection at the requested limit, and shares one structural
direct-source role predicate between assurance and return validation. Explicit
scope conflicts, full diagnostic topology, Main's mathematical selection, and
all Candidate/Fact boundaries remain unchanged. No scheduler, persistent
index, pagination layer, compatibility adapter, Candidate effect, or Fact
effect is introduced.

The 0.9.15 Frontier Exact Workflow Identity overlay separates semantic COW
completion from operational workflow projection. COW terminals decide only
completion or ambiguity; exact physical workgroup members retain their own
assignment, round, product, component, and supervision identities. The existing
validated product-to-source redirect remains available. No history is deleted
and no scheduler, compatibility layer, Candidate effect, or Fact effect is
introduced.

The 0.9.14 Typed Supervision Coverage overlay gives Candidate enforcement and
Main's ordinary/Campaign frontier one exact component-and-scope coverage
projection. Completed, missing, pending, conflicting, and unsafe states remain
distinct; malformed or inaccessible graph bytes alone are lineage-unreadable.
Candidate still requires one completed ingested result for every applicable
scope. The projection is advisory nontruth context with no scheduler,
automatic dispatch, compatibility layer, Candidate effect, or Fact effect.

The 0.9.13 Repair and Review Lineage overlay uses existing structural types for
worker products, supervision plans/results, and repairs while leaving relation
labels as Main context. Supervision receipt targets may be a unique subset of
additional exact related context. A repair-selected Main synthesis advances
only through a complete research-source/related-id match; workgroup completion
still requires a complete product, hash-bound repair objective, unique route,
and applicable supervision. `round-status` exposes attacked source assignment
and product ids for Main's live reviewer comparison. No relation dictionary,
identity registry, automatic repair decision, Candidate effect, or Fact effect
is introduced.

The 0.9.12 Frontier Lifecycle Closure overlay recognizes one structurally exact
historical Main-authored COW completion shape, gives an exact in-flight branch
goal-level precedence without removing sibling reconciliation, and reuses
already hash-validated Research envelope bytes before full record/artifact
validation in the same command. Main resumes a still-running host session
instead of issuing a duplicate mutating plan. No compatibility adapter,
automatic selector, hidden branch deletion, persistent index, Candidate effect,
or Fact effect is introduced.

The 0.8.0 MathGraph-first rule supersedes procedural compatibility language in
the historical overlays below: future upgrades need not preserve runtime
identity, adapter, migration, or administrative-gate protocols. Current
operations remain valid when graph identity, hashes, dependencies, provenance,
workflow stage, and owner boundaries are interpretable. The historical rows
record what older releases did; they are not current graph-operation gates.

The 0.9.11 Frontier and Source Continuity overlay keeps routine Campaign
frontier context bounded but mathematically decision-useful, extends exact
target-bound head handoff through completed terminal lineage, and makes
all-round discovery distinguish private atomic staging from visible malformed
entries. Source-scope cards may follow an explicitly related Research
product/review to its completed source-scope sibling and receive only primary
hashes actually cited by that review. There is no semantic graph scan,
automatic selector, compatibility layer, workflow gate, or truth effect.

The 0.9.10 Terminal Seal Hygiene overlay separates two exact Finder
`.DS_Store` locations from terminal authority while keeping the no-follow tree
inventory and the exact sealed path/hash set strict. Unknown entries,
unexpected directories, writable authority bytes, links, special files,
device crossings, and byte drift still fail. No product rewrite, adapter,
migration, scheduler, or truth effect is added. The same overlay shares one
ephemeral exact-snapshot Research COW/repair, completion, and product/receipt
inspection context inside an ordinary frontier command. Mutation invalidates
the context; no persistent index, output change, selection effect, or truth
effect is added.

The 0.9.9 Frontier Successor Handoff overlay lets one exact positive Research
dependency carry Main's already-expressed branch choice across overlapping
production and supervision. The predecessor review remains ordinary workflow;
only the duplicate semantic head is retired. Nonadvancing relations and
unrelated parallel heads are conserved, with no scheduler or truth effect.

The 0.9.8 Frontier Working Memory overlay lets Main express a Campaign-target
choice once at production planning. One compact replaceable nontruth map stores
bounded active, recent, landmark, and recovery Research ids; routine reads
hydrate exact mathematical summaries, and sparse Main updates handle genuine
branch/history curation. Structured source-evidence files keep their primary
role through source-scope projection. No automatic selector, scheduler,
compatibility layer, Candidate effect, or Fact effect is added.

The 0.9.7 Frontier Continuity overlay follows exact deep production,
supervision, repair, and COW lineage while retaining one stable workflow root
for an ingested product and its in-flight review. Active-goal workflow remains
primary; goal-unmapped Campaign attention is a bounded secondary summary.
Candidate identity is a read-only exact-byte projection, and CHX continuation
recovery is Main guidance rather than a monitor or gate. No selector,
dispatcher, checkpoint writer, compatibility layer, or truth effect is added.

The 0.9.6 Frontier Freshness overlay makes ordinary Main navigation typed over
Research and Facts and exposes a read-only checkpoint freshness comparison.
Productive terminal routes, clean review evidence, stale head ids, and a manual
refresh recommendation are distinct. No fuzzy selector, automatic checkpoint,
dispatcher, persistent index, or truth effect is added.

The 0.9.5 Exact Frontier Context overlay projects exact uncheckpointed terminal
production, supervision, and COW successors below attained Campaign checkpoints
for Main's judgment. New task cards also freeze valid existing Research ids
literally named in their source prose with exact artifact capabilities. Neither
surface infers relevance, mutates a checkpoint, dispatches, or changes truth.

The 0.9.4 Campaign History Compaction overlay keeps complete append-only
Campaign history but bounds routine status, checkpoint, and planning
projections. Scope-2 commits an exact ordered prefix without embedding its
bodies; new checkpoints carry routing references only; repair nodes and rounds
inherit one exact source Campaign. There is no paging subsystem, history
migration, automatic dispatch, or truth effect.

The 0.9.3 Integrated Frontier Cleanup overlay replaces the former parallel
advisory command family with atomic Campaign-bound Research creation and the
ordinary frontier. New rounds freeze exact selection receipts and replay argv;
attention fields expose unfinished or disposed work; live supervision is not
masked by new-suggestion safety. CHX liveness is explicit and historical
inventory is bounded with non-resolving COW administrative dispositions.

The 0.9.2 Frontier Semantic Choice overlay keeps active Campaign goals visible
after selected heads finish and returns semantic continuation or closure to
Main. Production mode remains a sparse automatic supervision hint; Main may
explicitly select any registered materially applicable primary scope for the
complete frozen successor. Neither surface dispatches, closes, or admits.

The 0.9.1 Campaign Frontier Heads overlay keeps each Campaign Research root as
immutable provenance while Main's newest append-only nontruth checkpoint names
bounded current work heads and attained boundaries. Routine reads start from
those heads; empty, multiple, and stale sets stay visible for Main judgment.
There is no automatic updater, migration, index, scheduler, or truth effect.

The 0.9.0 Frontier Active Fix overlay gives Main one compact decision surface:
exact Campaign `research_goal` progress beside stage-aware workflow actions.
Goals remain nonselecting hints unless Main supplies explicit Campaign scope.
The same release makes validation-profile cost visible, treats one successful
same-manifest forensic matrix as covering routine evidence, checks factual
repository release metadata explicitly, and leaves exactly one unqualified
installer entrypoint. Installation and publication remain CHX observation
surfaces without a post-release ceremony, monitor, scheduler, state machine,
compatibility layer, or truth effect.

The 0.8.12 Semantic Recovery overlay makes frontier completion follow one exact
canonical COW chain from a plan through its obligation-complete production
product and explicit invalidator to a unique current repair terminal. Terminal
invalidation or ambiguous lineage reopens the original workgroup. Main owns
cross-round search and final dispatch, uses free slots only for real independent
targets, and treats client reconnect as transport recovery followed by ordinary
agent/return/ingestion/round inspection. No compatibility adapter, persistent
projection, filler quota, scheduler, monitor, receipt, reconnect gate, or truth
path is introduced.

The 0.8.8 Direct Graph Operations overlay treats an admitted Fact and a frozen
primary source as direct graph capabilities. Ordinary Research validates only
the exact admitted Fact premises it names through their immutable local
Release/marker/Decision/event/interface/Fact bindings plus revocation
visibility; explicit closure reconstruction and attack targets keep the broad
path. `source_uses` accepts either a returned artifact or an exact task-card
related artifact whose role declares the standalone token `primary`, while toy
checks and bridges remain return-bound. This is one semantic rule for existing
and future nodes, with no compatibility layer, version branch, cache, receipt,
or new authority state.

The 0.8.7 Main Observation and CHX Snapshot overlay completes the current
orchestration instruction: Main directly notices both ordinary visible worker
progress and the end-to-end elapsed time of commands or work units already
shown by the host. Unexpectedly long duration relative to apparent complexity
or a recent comparable operation is a diagnostic signal, not an automatic
timeout, failure, lifecycle state, or admission gate. No timer, daemon, watcher,
polling loop, heartbeat, numeric threshold, persistent performance state,
receipt, or compatibility layer is introduced.

The same overlay removes open-ledger status as a global-CHX-repair gate. The
repair remains bound to one exact current inventory and every already observed
qualified issue. Covered open-ledger mutation stales the prior snapshot, while
malformed lineage, report drift, incomplete issue coverage, competing
cross-branch `supersedes`, candidate drift, and evidence drift remain strict
errors. This changes nontruth architecture accounting only.

It also removes reproduced administrative expansion at existing boundaries:
an independent Research append with no Fact dependencies does not reconstruct
active Facts; selective checkpoint targets share one ephemeral inspection;
exact supervision retry filters unrelated manifests and returns before
rebuilding planner Research; mtime/ctime-only capability localization does not
override exact bytes and SHA-256; and current worker commands execute the bound
`scripts/mgraph` shell entry. There is no persistent cache, new runtime state,
compatibility adapter, monitoring service, or truth-authority change.

Supervisor cards also project capabilities by selected scope instead of
copying every production baseline: proof/program/integration retain the exact
production card, applicable outputs, and active Fact premises; source-scope
retains the exact source closure. Historical Research artifacts with exact
project-relative path/SHA-256/role structure are capabilities by graph
semantics, not by a runtime-era assurance label.

The 0.8.6 Bounded PHX Repair overlay made the remaining orchestration explicit:
Main selects load-bearing Research ids and separately launches/confirms workers
after planning; Main-authored canonical Candidate Fact bytes are valid inputs
while provenance remains nontruth lineage; canonical return validation is a
single snapshot with retryable transient visibility; performance observation is
Main-visible only with no timer, daemon, or threshold; and Gateway alone owns
`certification-record`. No compatibility layer, persistent monitor, or new
mathematical gate is introduced.

This matrix governs the 0.3.6-to-0.4.0 kernel upgrade, the additive 0.4.1
CHX-ledger patch, the original opt-in 0.4.2 adverse-routing release, its later
prospective-default reporting revision, the prospective 0.4.3 assurance and
routing repairs, and the coordinated 0.6.0 research-draft, composable-
verification, CHX-accounting, and advisory L3/L4 repair.
The coordinated 0.6.1 overlay changed no graph-plane authority. It added only
terminal historical-runtime continuity:
host-managed content objects and identity records outside skill discovery,
component-wise no-follow validation, exact sealed-tree revalidation, and
bounded-phase scan deduplication. Active work and every mutation remain
full-manifest exact-current-runtime operations; round planning performs that
preflight before any project snapshot, round, or task-card write.
The coordinated 0.6.2 overlay adds a prospective Paper Research Pipeline. It
inherits the complete Paper DAG copy-on-write, preserves premise order and
role-typed topology, binds publication identity and claim-level witnesses,
normalizes heterogeneous deltas, and requires a Paper-subject atomic preflight.
It added no graph-plane authority; its historical advisory surface is retired
in 0.9.3.
The same release binds every new runtime module and public CLI into one shared
compatibility-closure validator used by self-test and the preservation suite
(CHX-053), preventing manifest success from hiding a stale protected-tree hash.
It also recomputes exact evidence/successor receipt identities at every Paper
preflight and provides a domain-neutral real-pipeline reliability matrix across
graph, frontier, atomic DAG, stance, evidence, and successor surfaces
(CHX-054).
The coordinated 0.6.3 overlay changes no graph-plane authority. It makes
ordinary Paper-continuation status a bounded, content-addressed indexed read
with explicit `--full` detail and an explicit full-closure index rebuild,
upgrades new CHX ledgers to transitive lineage revision 4, and upgrades public
disclosure to exact per-run issue ownership across immutable successor ledgers
and makes every exact-runtime host entrypoint non-writing before local imports
(CHX-058 through CHX-062).
The 0.6.4 and 0.6.5 releases historically carried goal-intake and advisory BF
surfaces. Version 0.9.3 retires that parallel command family without a
compatibility layer: explicit Campaign operations plus atomic
`memory-add --campaign` and the ordinary frontier preserve the useful semantics.
It keeps deep research effort independent of necessity-based changed-
surface validation; adds mathematics-specific exact-target/weaker-progress
contracts, paired independent adverse Research, bounded Paper release capsules,
host-controlled neutral-review return, and executable behavior-orphan lifecycle
decisions. Newly started CHX ledgers use revision 5: project-local per-ledger
resolution uses one prior full-tree reconnaissance receipt, one reusable
tactical repair per resolved issue, and a latest per-ledger integration; a
globally installed repair uses the direct cross-ledger global integration with
no tactical precursor. Revisions 1–4 preserve their
original contracts (CHX-069–081).
The 0.6.6 Supervised Research Cycle overlay changes only prospective Research
orchestration. Public rounds become a frozen production subround followed by
one to three scoped supervisors. The 0.6.8 Research Supervision Stability
overlay reserves formal Research refute work for that second subround: public
production is constructive-only, while whole-Candidate adverse review and the
verifier remain separate. Admitted Fact dependencies are frozen premises unless
exact new conflict evidence is escalated for authority-governed reopening.
Supervision validation is cycle-safe and identical retry reuses a
content-addressed pre-round Research write. Computation freezes core code,
design, and dependencies before
program-math supervision, permits execution only after a safe explicit
disposition, requires exact source/dependency hashes, and supervises actual
output afterward. Pulse is not invoked, legacy cards are not backfilled, and
Candidate, Certification, Gateway, Fact, Evidence, Paper, and Reader contracts
are unchanged.
The 0.6.9 Research Gate Integrity overlay freezes deterministic supervision
components from exact Research ancestry and lets a complete component enter
review while unrelated work continues. Candidate checks the complete live
supervision set before expensive work and again under the seal lock. Formal
computation ignores aborted reviews and revalidates its exact supervision plus
latest disposition under the execution-round lock. These are prospective
Research gates; they do not change Candidate schema or Fact authority.
The 0.8.0 MathGraph First overlay makes graph semantics the cross-version
compatibility basis. Content hashes, dependency references, creation/session
provenance, workflow stage, and owner boundaries remain exact; runtime identity,
installation path, historical archive, and obsolete receipts are diagnostic
provenance only. Legacy graph records remain directly operable without an
adapter, migration copy, or mode-init ceremony. This removes redundant
ingress/runtime gates only; production, supervision, adverse, verifier,
Candidate, Certification, Gateway, and Fact boundaries remain unchanged.
The 0.7.0 Failure-Informed Efficiency overlay keeps those atomic gates and
replaces only broad prospective administration. A static append-only registry
selects supervisors for reproduced program-math projection, proof-boundary,
source-applicability, and cross-output-contract failures. Plain interpretive
insight has no blanket proof review, and integration is reserved for components
crossing distinct primary scopes. New cards default to one Blackboard root node
with zero writes; larger reads and all writes require explicit capabilities.
New V5 Pulse planning is retired while V4 and existing-record completion remain
compatible. A bounded already-active Learner oral follow-up skips unrelated
research architecture only when it is read-only and has no truth, persistence,
or external effect. Candidate, fresh adverse, verifier, Certification, Gateway,
Fact, exact-runtime, abort, and lock-held final-write gates are unchanged.
The 0.7.1 Scoped Supervisor Bootstrap overlay changes only the startup
projection for an exact current second-subround card. A managed supervisor reads
the full skill, exact prompt/card, and one manifest-bound compact contract;
broader computation, source, attack-learning, CHX-repair, or unprojected
assurance protocols load only at explicit fail-closed branches. Production and
legacy workers retain the ordinary full protocol. Task-card and runtime
validation, worker CHX, return schema/preflight/hash ingestion, repair rounds,
Candidate fresh adverse, verifier, Certification, Gateway, and Fact admission
are unchanged.
The 0.7.2 Integrated Research Efficiency overlay adds the parallel compact
production projection, one command-local inspection context for repeated
supervision-planning reads, and one shared canonical computation-design role
set checked before round creation. The context is never persisted and final
lock-held overlap, abort, runtime, authority, and liveness checks remain fresh.
Task-card/runtime validation, return preflight and ingestion, Candidate fresh
adverse, verifier, Certification, Gateway, and Fact admission are unchanged.
The 0.7.3 Selective Startup overlay removes work that exact scope already makes
unnecessary. Explicit Research ids validate only their immutable ancestor chain
plus typed invalidation and disposition events, rather than rebuilding the
scored global frontier. Repair Research freezes its production work mode, so an
abort/replan cannot silently change compute or literature into prove. A missing
or blocking program-math disposition rejects execution before design-artifact
closure is rebuilt, while the final lock-held authority recheck remains. Exact
worker cards bind their CHX ledger; ingestion projects only genuine findings
from a closed matching ledger into a small nontruth observation inbox. An empty
ledger or a mathematical challenge creates no observation. A third fail-closed
compact selector covers bounded edits of an existing Learner teaching document.
Ordinary Research may leave negative source status unassessed, and one-off
computation retains only the production validation path plus the smallest
independent mathematical check unless a recorded failure family selects more.
These changes add no scoring, Pulse, Blackboard, background daemon, truth
authority, or Candidate/Fact shortcut.
Its later administrative-read repairs also change no graph-plane authority.
One aggregate inspection context reaches every subaudit; applicability is
decided before an empty authority or absent optional subsystem opens expensive
validation; one canonical immutable collection is validated before filtered
projections are derived. The canonical Experiment root and project-owned shared
snapshot lock close CHX-106–107. These are command-local repairs, not persistent
indexes, audit shortcuts, or new truth claims. The accompanying PHX ledger is a
private host-global, nontruth route reference for significant reusable architecture
proposals: concrete problems remain in CHX, and any active-architecture adoption
requires prior informed user consultation plus digest-bound evaluation.
CHX-108 additionally binds the public PHX standalone parser to exact per-command
handler arguments and tests `start` plus `search --write-receipt` through real
subprocess dispatch.
CHX-109 requires present optional state to consume a complete derived owner view
from the canonical frozen binding; exact absence alone retains the bounded skip.
The 0.7.12 Admission Inspection Reuse overlay extends command-local reuse to
Fact admission. One complete ephemeral context is shared before publication,
then discarded; a distinct fresh context is created under the mutation lock for
historical-release and lineage replay. No authority cache crosses the lock, and
the Candidate, verifier, Certification, Gateway, atomic marker, and Fact
contracts are unchanged.
The 0.7.13 Admission Frontload overlay separates dependency-closed authoring
batches from Fact atomicity. Each prospective Fact exposes one semantic
conclusion and multi-Fact batches bind exact internal DAG edges. This cheap gate
runs before global Research replay; new `candidate_fact` returns are canonical
Fact-validated before ingestion, and exact Main repair specifications are
hash-bound into repair Research and task cards. Historical returns retain their
frozen contracts and the later truth gates remain unchanged.
The isolated prospective 0.4.4 `Back to the Future` candidate restores two bounded context-planning
connections: exact promoted-query snapshot selection and enum-only mode hints.
It also replaces full background bodies in new cards with an exact retrievable
index. The copy-on-write field repair adds source capabilities, runtime-bound
worker ledgers, adverse-assignment provenance, nonrecursive admission recovery,
legacy-premise witnesses, and explicit read-only prior-Fact inventory. Historical
cards and running work keep their frozen contracts.
The isolated 0.5.0 Campaign successor connects only explicit exact-match V5
frontier scope plus a bounded frozen nontruth envelope; unscoped Main behavior,
active-Campaign defaults, frozen work, and Fact admission remain unchanged.
Exact Chalxius 0.4.0 project/runtime behavior remains the compatibility
baseline for projects and frozen work units without the new activation
contract. The four-state V5 model constrains only the
truth-bearing critical path; it is not a license to delete unrelated product
capabilities.

## Decision rule

- `keep`: preserve behavior and authority boundary; allow implementation cleanup only.
- `adapt`: preserve the user outcome while changing an issue-linked coupling or storage interface.
- `replace`: allowed only with cited CHX evidence and an equivalent-or-stronger regression.
- `remove`: allowed only after explicit user discussion plus cited CHX evidence.

No issue mapping means `keep` or interface-only `adapt`.

Behavior-orphan lifecycle is more exact than this release-action vocabulary.
`normal_flow` must `retain_and_integrate`; explicit manual, Host API, and
compatibility surfaces may `retain_bounded`; dormant duplicates must
`replace_with_authoritative_mechanism` or retire; deprecated surfaces must
`retire`. The behavioral registry names the replacement and boundary probe, and
the executable gate rejects missing, incompatible, cyclic, or self-replacing
decisions. Import or test reachability alone is insufficient.

Original Danus versions, including 0.2.9 and later releases, are behavioral references only. The
candidate consumes no source module, runtime, writer, fact, review, receipt, or mutable store from
them.

## Public capability inventory

| Capability | 0.3.6 public commands or surface | V5 action | Issue mapping | Required preservation evidence |
|---|---|---|---|---|
| Project and Fact reading | `init`, `status`, `audit`, `show`, `search`, `closure`, `context`, `targets`, `set-targets`, `export-mermaid`, `fact-graph-inventory`, `fact-graph-append-target` | keep/adapt | CHX-039, CHX-040, CHX-048, CHX-100–107; field CHX-004 | empty V5 Fact start; V4 readable nontruth; complete audit threads one project-owned aggregate inspection context through every subaudit under the shared snapshot lock and reuses only successful exact immutable projections inside that command; routine status is a bounded non-forensic dashboard and `--with-audit` preserves the full validator; cost repair eliminates work before command-local reuse and measurement, with persistent indexing or parallelism last; explicit prior-V5 inventory/append-target routing with no import or federation |
| Procedurally reserved 0.x single-Fact certification | historical commands `submit`, `packet`, `make-verifier-task`, `record-review`, `admit`, `revoke`, `candidate-release`, `candidate-release-check`, `verifier-capsule`, `certification-record`, `certification-decision-check`, `fact-admit` | retain bounded compatibility for explicitly selected 0.x completion/audit | CHX-009, CHX-021, CHX-024, CHX-025, CHX-032, CHX-039, CHX-048, CHX-058, CHX-068, CHX-076–077; field CHX-005–006 and CHX-011; `run-20260730T043905300313Z-41247ad11966/CHX-002–003`; `run-20260812T200026180866Z-ccb9d355783f/CHX-006–008` | frozen historical checks remain available when Main explicitly selects the 0.x route; the runtime does not authenticate pre-1.0 provenance and no identity gate is added; these commands are not current Fact Alpha certification authority |
| Procedurally reserved selective Fact-admission checkpoint | Main-only `selective-fact-checkpoint` | retain bounded 0.x nontruth compatibility | `run-20260812T134234791817Z-ae0fd96bef2e/CHX-001`; successors `run-20260812T164021036787Z-e3fa69b57a73/CHX-002`, `run-20260812T175055302224Z-943735ad5337/CHX-005`, and current CHX-013 | preserves its exact frozen target/exclusion, dependency, and batching contract only for explicitly selected historical completion/audit; the runtime does not authenticate pre-1.0 provenance; no current package, verifier, Gateway, or Fact effect |
| Fact Alpha Research certification | Main `fact-frontier-mark`, `fact-frontier-dispose`, `fact-frontier`, `plan-fact-packaging`; packager `fact-package-seal`; independent verifier `fact-verifier-capsule`, `fact-verification-record`, `fact-verification-check`; Gateway `fact-certify` | additive prospective authority overlay | 1.0.3 integrated implementation | one immutable Research graph; sparse Main landmarks and marks; `--target` requires `--campaign`; route-needing landmarks rank before certified history with complete identity count/digest beyond the 256-item preview; supervisor `ready`/`needs_split` owns Research review and strong mechanical advice only; all-ready planning returns `mechanical_proposal_ready` with `mechanical_package_proposal` and digest but does not seal; the packager seals once and may accept it, provide an alternative six-field interface, select a whole node, or choose an ordinary COW branch without changing the Campaign active head; an actual split pauses packaging for a Research repair worker and ordinary proof/source whole-batch supervision, never a Fact-side split supervisor; the verifier checks every exact Research record, interface, and dependency edge regardless of author; verifier records are nontruth and Gateway alone certifies; the historical fast lane is only bounded one-to-one complete-node minor COW with full same-verifier component recheck, while split/structural ambiguity returns to ordinary Research; no timer, scheduler, duplicate adverse stage, provenance gate, legacy authority migration, or mathematical split-completeness inference |
| Frozen 0.x Candidate adverse completion | historical Main commands `prepare-candidate-adverse-target` and `plan-candidate-adverse` | procedurally reserved compatibility | `run-20260812T175055302224Z-943735ad5337/CHX-003`; `run-20260813T083233526366Z-39d8bc3904ff/CHX-015–016`; superseded prospectively by Fact Alpha | an explicitly selected historical Candidate route may finish its frozen contract; the runtime does not authenticate pre-1.0 provenance and no new gate is added. No new 1.0.x package gains a Candidate-adverse stage. Current certification uses one packager-selected nontruth package, one full independent verifier review, and Gateway-only certification |
| Atomic FactBundles | `fact-bundle-submit`, `fact-bundle-record-review`, `fact-bundle-admit` | keep/adapt | CHX-022, CHX-029, CHX-030, CHX-034, CHX-039 | all-or-none mini-DAG visibility without profile-closure coupling |
| Research memory and repair | `memory-add` including `--current-assurance`, `memory-update`, `frontier`, `plan-repair-round` with optional `--input SPEC.json` | adapt | CHX-011, CHX-013, CHX-015, CHX-030, CHX-039, CHX-041; current CHX-014; field CHX-001 | cumulative Research Ledger; exact source capabilities for source-dependent planning; counterexamples and insights survive later rounds; compact four-factor automatic ordering with legacy eight-metric read projection, no cutoff, and explicit low-score scheduling; optional exact bounded repair specification is normalized and hash-bound into repair Research and the task card; a bound source passes its exact Campaign into both repair Research and round scope, while an unbound source never infers `ACTIVE` |
| Historical adverse routing compatibility | no prospective public commands; conditional fields only on frozen historical task cards/returns | retire dynamic learning; retain bounded historical completion/readability | CHX-078–079; field run `run-20260805T014916949124Z-924b0053d92f/CHX-001`; 1.0.3 integrated simplification | current production and supervision use fixed, scope-owned, non-exhaustive review guidance; proof owns mathematical applicability and source owns external fidelity/coverage; prospective cards contain no `adverse_routing`, returns add no `attack_learning`, and ingestion creates no prospective case/proposal/rule/report. Explicitly selected frozen rounds may finish under their exact old schemas and append the records that contract required; the runtime does not authenticate pre-1.0 provenance. Historical rule text is inert searchable nontruth context with no current planning, package, verifier, Gateway, or Fact effect |
| Novelty evidence | `novelty-record`, `novelty-status` | keep | CHX-020, CHX-033, CHX-102 | query/corpus/date-bounded status; aggregate novelty audit uses the owning inspection context for exact Research, Fact, revocation, Release, admission, and Paper reads; no automatic campaign/paper expansion |
| Three-plane task execution | `plan-round`, `plan-supervision-round`, `plan-computation-execution`, `round-status`, `preflight-return`, `validate-return`, `ingest-return`, `work-unit-abort` plus immutable task cards | adapt | CHX-004, CHX-006–013, CHX-016, CHX-019, CHX-024–028, CHX-031, CHX-032, CHX-034, CHX-035, CHX-037, CHX-052, CHX-059, CHX-060, CHX-062–067, CHX-097, CHX-103; `run-20260728T180420869461Z-5d4e882a1c9c/CHX-001`; `run-20260729T081913970946Z-2f0a9e345447/CHX-001`; `run-20260730T043905300313Z-41247ad11966/CHX-001`; field CHX-002–003; candidate run `CHX-001`, `CHX-004`; release CHX-027–031; `run-20260809T083132685134Z-ef62c2588193/CHX-001–007` | control/state/narrative planes remain distinct; new public rounds freeze constructive-only production outputs and deterministic Research-ancestry logical components before sparse proof/program/source/integration supervisors; one completed component may enter supervision while unrelated workers continue, but a related component and its integration supervisor wait for complete hash-valid Research products (the historical `source_receipts` label is retained for descriptors); worker-ingestion receipts are optional workflow markers, not product capabilities or mathematical authority; multi-component calls require a frozen component id, timing-derived subsets and overlapping scope coverage fail closed, and historical component-free rounds retain whole-round waiting; production rejects explicit or auto-selected refute and appends no adverse pair; supervisors attack new Research while admitted Facts remain frozen premises absent exact conflict escalation; Candidate has a cheap live-supervision gate and recomputes the exact result set under the seal lock; first-wave compute returns exact code/design/dependencies without executing; only safely disposed live non-aborted assignment-local program-math supervision gates same-hash execution, authority is rechecked under the execution-round lock, and actual output returns to supervision; Pulse is inactive and defects use later copy-on-write repair; one ephemeral binding-keyed inspection stack prevents recursive supervision validation without skipping the outer authority/hash reconstruction, and content-addressed supervisor Research makes partial-write retry idempotent; legacy direct rounds remain single-wave; runtime metadata is optional diagnostic provenance and does not gate round access; product-complete cards retain graph-level return/Research state and optional worker receipt provenance without requiring a current runtime or historical archive; one identical identity is scanned at most once per bounded phase without cross-phase authority caching; complete source Research dossier; applicability-before-authority produces the canonical empty snapshot only when exact dependencies, related Fact, and attack target are all absent, while every nonempty or malformed case retains complete authority validation; task-referenced current-authority snapshot with exact target capabilities; outcome-independent adverse assignment provenance; Main-planner-compiled context receipt; prospective marker and old-card compatibility; public exact V5 return schema, template, CLI/prompt pointers, and actionable missing/unknown obligation diagnostics; local quarantine; validated abort authority projects unfinished assignments as `frozen_aborted`, exposes the abort id, and leaves zero live awaiting returns; aggregate status and audit reuse one ephemeral inspection context through the same public `round_status` projection, so stale public output fails strict audit without repeated runtime or quarantine scans |
| Historical runtime archive and cutover | `scripts/archive_runtime.py`, `scripts/runtime_cutover_project_validation.py`, `scripts/runtime_cutover.py`, schema-2 `runtime_content_sha256`/`historical_archive_root`, schema-1 identity registry | additive deployment diagnostics | release CHX-027–034; current CHX-062, CHX-067, CHX-084–086, CHX-090–094 | archive and cutover remain release/rollback tooling outside graph authority; archived code is never imported or executed; project validation, rollback, final TOCTOU, and manifest checks protect deployment integrity only; runtime identity and archive location are not required for ordinary graph reads, Research continuation, or graph writes; no Paper, Evidence, Research, Candidate, Certification, Fact, CHX, Reader, Learner, Campaign, or L3/L4 authority |
| Release validation matrix | `scripts/release_validation.py`; `scripts/aggressive_bug_audit.py`; optional forensic `scripts/architecture_reconnaissance.py` and `scripts/behavioral_feature_gate.py`; explicit `--metadata-only --repository-root` | additive release validation | current CHX-064–065, CHX-068, CHX-080–083, CHX-088, CHX-096, CHX-098–099; 0.9.0 CHX-006–009; extends field CHX-014 | Main chooses one profile for one exact manifest; routine runs isolated self-test, changed-surface tests, and the semantic mutation audit, while forensic runs the full suite, behavior registry, whole-tree reconnaissance, and complete mutation registry; successful forensic evidence explicitly subsumes routine only for unchanged bytes; the receipt exposes elapsed, lane-seconds, and slowest lane; a later metadata-only projection checks repository version/name/archive/manifest/checksum facts without authoring prose; release evidence only, no truth effect |
| Blackboard exploration | `blackboard-space-create`, `blackboard-node-add`, `blackboard-edge-add`, `blackboard-type-register`, `blackboard-show`, `blackboard-query`, `blackboard-snapshot`, `blackboard-snapshot-query`, `blackboard-reindex`, `blackboard-promote-node` | keep/adapt | CHX-018, CHX-026, CHX-027, CHX-030, CHX-039–041 | typed cumulative nodes/edges; immutable snapshots; no automatic Fact promotion |
| Pulse collaboration | `pulse-plan`, `pulse-status`, `pulse-barrier`, `pulse-dispatch`, `pulse-void`, `pulse-close`, `pulse-abort`, `pulse-audit` | adapt | CHX-001–003, CHX-005, CHX-014, CHX-015, CHX-017, CHX-018, CHX-022, CHX-023, CHX-027–031, CHX-036, CHX-037, CHX-039, CHX-041, CHX-048, CHX-052 | same-snapshot Wave 1 and snapshot-mediated Wave 2 remain; each contribution ingests independently; no peer destruction |
| Reasoning profiles and adoption | `mode-init`, `mode-status`, `mode-switch`, `adoption-plan` | adapt | CHX-005, CHX-020, CHX-033, CHX-036, CHX-039, CHX-048, CHX-069 | fast/auto/deep remain exploration budgets; deep increases applicable research depth but validation scope is necessity/changed-surface based, reusing current receipts for unchanged sealed state and failing to full validation on drift or ambiguity; adoption remains advice and neither creates universal certification closure |
| Process readiness (V4 profile-closure compatibility) | `profile-closure-status`, `profile-closure-record` | adapt; user-approved 2026-07-28 | CHX-014, CHX-023, CHX-029, CHX-036, CHX-039, CHX-048 | retain and strengthen repair suggestions; recording appends guidance to Research only and has no admission authority |
| Campaigns and active frontier | `campaign-create`, `campaign-activate`, `campaign-status`, `campaign-target-add`, `campaign-target-archive`, `campaign-update`; atomic `memory-add --campaign`; explicit `frontier --campaign`, `plan-round --campaign`; Main `goal_coverage`/`workflow_queue` surface | adapt; 1.0.3 current contract plus frozen earlier repairs | CHX-020, CHX-033, CHX-039, CHX-063, CHX-070, CHX-010–012; 0.9.3-0.9.6 integrated CHX | optional durable multi-round envelope with exact Campaign-bound roots and active heads; sparse landmarks have no count quota and grow with the Research graph; recent attainment is curated by context compaction or natural curation rather than a four-event gate, with 64 as its high-water runaway protection; routine summaries preview four recent items and eight landmarks while retaining exact total counts, identity digests, and full diagnostic access; current COW/split routes, exact plan-round argv, and manual ambiguity remain visible; complete state stays addressable without automatic dispatch, close, fuzzy selection, migration, persistent index, retagging, second scheduler, or truth effect |
| Claims, conventions, and exposition | `claim-add`, `claim-show`, `claim-variant`, `convention-add`, `convention-show`, `export-claim-card`, `export-interpret-card`, `publish-interpret-document`, `lint-expert-document`, `lint-interpret-document` | keep | CHX-006, CHX-007, CHX-009, CHX-010, CHX-016, CHX-019, CHX-024, CHX-034 | exact interfaces, quantifiers, conventions, and lint semantics remain available |
| Replayable computation | `experiment-start`, `experiment-event`, `experiment-observe`, `experiment-decision`, `experiment-status`, `experiment-resume`, `experiment-finalize` | keep/adapt | CHX-039, CHX-048, CHX-054, CHX-059, CHX-066, CHX-067, CHX-105, CHX-106, CHX-109 | immutable commands/artifacts/checkpoints and independent replay remain; current V5 stages bind formula, domain, representation, approximation budget, output meaning, and independent checks; the optional Experiment root is derived from the canonical frozen assignment and inspected component-wise without following symlinks; exact absence skips only that subaudit, while presence materializes the complete validated task-card view from the existing round binding before any manifest, hard-cap, event, or receipt read; unsafe roots and state fail closed; task-local binding only |
| Paper Logic, Audit, and continuation | `paper-logic-init`, `paper-logic-stage`, `paper-logic-record-review`, `paper-logic-freeze`, `paper-logic-status`/`--full`, `paper-logic-show`, `paper-logic-query`, `paper-logic-audit`, `paper-logic-link-exploration`, `paper-logic-project-blackboard`, `paper-continuation-plan`, `paper-continuation-status`, `paper-continuation-status-index-rebuild`, `paper-continuation-dispose` | keep/adapt | CHX-033, CHX-038, CHX-043, CHX-058, CHX-061, CHX-069, CHX-072, CHX-075–076, CHX-100, CHX-104; `run-20260730T145847217907Z-eedfc9368ea0/CHX-001–003` | source/reconstruction/audit separation and append-only correction; all-target, bounded-target, or exact changed-surface continuation preserves complete directional closure without score compression; native stage/freeze rechecks successor head/topology/premise order; routine status consumes atomic HEAD/receipt and ingestion commits resumable head-last status transitions; one command-scoped validated Paper/Research projection is shared across aggregate audit without persistent indexing; the canonical immutable disposition collection is validated once per manager inspection before plan-specific and current views are filtered from it; current Candidate release materializes one content-addressed capsule over status generation, Logic/Audit refs, source/writing, closure, dispositions, and interfaces, reused through verifier/Certification/Gateway; stale/ambiguous capsule state emits one observable full-validation fallback; philosophy-only atomicity/clarity remain separate from domain-general Paper adequacy and Fact truth; historical releases preserve old evidence shape |
| Research-draft admission | `research-draft-plan`, Operator-only `research-draft-authorize-major-revision`, `research-draft-disposition-batch`, `research-draft-status` | additive prospective repair | field `CHX-001–012`, `CHX-015–017`, integration `CHX-020`, release `CHX-035`; CHX-074 | mandatory `research_draft` source role; full-artifact proposition, span, operator, attribution, and target inventories; one immutable plan plus one atomic target-total disposition batch; source-derived atomic components and explicit mini-DAG splitting; many-to-many Paper/Facts mappings; versioned domain closure profiles; only philosophy uses durable exact-plan/target/stance/impact Operator authorization; mathematics binds canonical frozen target ids plus immutable target/hypothesis/domain/quantifier state and a typed non-closing refinement DAG, so weaker results expose exact deltas and cannot substitute for an open root; empirical/mixed retain their own target adapters; exact limited-restorative stance canary remains philosophy-only across plan, authorization, disposition, release, and admission fixtures; source/Paper/schema-6/atomicity/failure-surface cross-plane binding; release-relative EvidenceRef transport; cached validated dependency receipt; Paper-subject Candidate Release, fresh composable verification, unchanged Certification/Gateway, and admitted Fact Graph before further graph-led Research; external finished work stays Evidence until an explicit bridge and fresh admission |
| Paper Research Pipeline | `scripts/paper_research_pipeline.py frontier`, `frontier-verify`, `successor`, `evidence`, `preflight`, `delta-normalize`, `reliability-matrix` | additive prospective repair | CHX-036–055; CHX-074 | current research drafts inherit their complete Paper Graph rather than detached prose or a convenience FactBundle; order-preserving no-cutoff frontier; stable-id copy-on-write successor; partition/atom hierarchy; exact source-occurrence accounting separate from logical operators; open-world exact operators; publication identity plus claim-level witnesses/reviews; exact receipt identity recomputation and whole-receipt preflight binding; corroborated PDF dehyphenation; normalized nontruth deltas; Paper-kind atomic DAG and domain-specific continuity; mathematics preflight requires an exact-target policy plus typed refinement DAG and records weaker progress as root-open; domain-neutral actual-data mutation matrix; native Paper review/freeze and the ordinary verifier/Gateway remain mandatory |
| Composable verification | `verification-key-register`, `verification-plan-prepare`, `verification-plan-record`, `verification-packet-prepare`, `verification-packet-record`, `verification-receipt-prepare`, `verification-receipt-record`, `verification-aggregate`, and `verification-status`; pure protocol in `scripts/mathgraph/parallel_verification.py`, project lifecycle in `scripts/mathgraph/parallel_verification_lifecycle.py` | additive prospective repair | field `CHX-014`; integration `CHX-021–026` | Operator-registers project-bound prime-order Ed25519 planner, Host, and reviewer keys; every new or idempotent registration and every registry read rejects one public key appearing under multiple key, role, principal, Host, or trust-domain identities; immutable-record cache hits revalidate the same complete registry and never cache authority; public status, subsystem audit, and top-level V5 audit enforce the same registry-wide invariant; the planner signs one exact release/capsule-derived immutable work plan before dispatch; risk-derived shards preserve all topology and high-risk overlap across distinct principals, hosts, and trust domains; Host dispatch and reviewer receipts are blind-to-peer, scope/time/key-bound, and use project-wide nonrepeating nonces; deterministic monotone aggregation rejects missing coverage, conflicts, new obligations, duplicate role authority, stale bytes, invalid signatures, and release drift; strict research-draft Certification and Gateway admission revalidate the same eligible aggregate; adjudication remains separate and the ordinary Gateway remains the sole admission authority |
| Cross-project Evidence | `evidence-library-status`, `evidence-query`, `evidence-sync-retry`, `evidence-import-fact-graph`, `evidence-bridge-prepare`, `evidence-bridge-check`, `evidence-mark`, `evidence-impact-report`; optional Candidate Release `evidence_bridge_refs` | additive isolated candidate | current Evidence run/CHX-001; compatibility canary/CHX-001; CHX-073, CHX-095 | reviewed Paper freeze auto-archives exact PDF/version/graph/reviews; non-paper Fact import is explicit user + Operator only; a scoped runtime-independent authority audit accepts supported older V5 sources without reopening frozen nontruth work while retaining exact Fact/Release/Decision/admission/revocation/event/interface/closure checks; exact certified Fact imports first persist one immutable `eap-*` planning attempt so pre-request Paper failures remain visible and retryable, then revalidate the local and library capsule, Fact Evidence record, source release, and exact Paper refs before materializing an `eas-*` effect; no title/DOI/credibility inference; Evidence stays nontruth and premise-ineligible; bridge is content-addressed, destination-bound, fresh-verifier and Fact-Gateway checked; correction stales future reuse and reports impact without silent Fact revocation; historical releases and running projects unchanged |
| Reader visualization | `export-reader-html`, packet v1, renderer revision 20 | adapt | CHX-042; this run/CHX-001 | backward-compatible packet validator; bounded V5 titles; hash6 plus role/plane canvas identity; MathJax-ready historical Fact summaries with exact source preserved; deterministic per-theme centers and membership-scaled equally spaced rings; presentation-only shared membership from strong prerequisite/support target closures; multi-field overlap placement; default-on session gravity; orbit-off collisions let only the impacted old Cartesian pin yield and update; bounded local animated equilibrium with no idle simulation; no authority effect |
| Project background | single `PROJECT_BACKGROUND.md` nontruth source; `project-background-index`, `project-background-read` | adapt | CHX-040, CHX-048, CHX-051, CHX-052; `run-20260728T180420869461Z-5d4e882a1c9c/CHX-001`; this run/CHX-001 | generation/refresh requires user instruction; new 0.4.4 cards freeze a complete exact-byte chunk index and immutable round snapshot instead of embedding the body; Main/Operator may inspect, Main-planner selection plus unselected counts are explicit, and the dedicated Host dispatch role is unchanged; every chunk remains hash-bound and rehydratable after context compaction; old cards keep their full-body schema; Reader compatibility remains; authority overrides background status prose |
| Migration and legacy import | `upgrade-project-copy`, `upgrade-workflow`, `import-danus` | adapt | CHX-040, CHX-044–047, CHX-051, CHX-052 | read-only historical access; user-directed summary generation; default reading when present; no V5 authority inheritance |
| Chalxius Learner | `scripts/learn`, `scripts/learning_graph.py`, academic routing, `references/learner_document_edit_bootstrap.md` | keep/adapt | bounded-document-edit startup CHX from task `019fa908-8cf5-7a70-a867-dde76a1c6182` | explicit activation, nontruth-only writes, frozen read-only mounts; an existing teaching-document edit uses a compact fail-closed selector and exits to the ordinary path on fresh research, truth, architecture, publication, persistence, or nonlocal editing |
| External-source and elementary gates | source/applicability validators and controlled elementary ledger | keep/adapt | CHX-033, CHX-064, CHX-065, CHX-069, CHX-071, CHX-072 | current source-evidence v4 binds complete hypotheses and conclusions, query evidence, structured status, and typed transports; v3 remains historical/readable |
| Verifier capability isolation and formal return | `scripts/prepare_verifier_capsule.py`, copied `host/submit_review.py`, `scripts/submit_neutral_review.py` | keep/adapt | CHX-039, CHX-041, CHX-058, CHX-061, CHX-068, CHX-070, CHX-071, CHX-077; `run-20260730T043905300313Z-41247ad11966/CHX-002` | exact allowlist, neutral materialization, complete decision template, one shared enumerated finding-class validator, fresh independent verifier, no exploration workspace access; new releases seal the prospective submission revision, the verifier writes a draft only, and host preflight quarantines invalid drafts with structured diagnostics before publishing an immutable canonical review plus content-addressed formal-return receipt; interrupted publication is idempotently recoverable and a failed draft has zero Candidate/Certification/Gateway/Fact effect; historical unmarked V5 and all V4 capsule returns remain byte-compatible |
| CHX runtime ledger | `scripts/chx_ledger.py`, mandatory start/close procedure, worker `start --task-card`; `finding`, `reconcile-finding`, `record-reconnaissance`, `record-tactical-repair`, `record-integrated-repair`, `record-global-repair`, typed issue relations, successor ledger, deterministic `report`/`verify-report`, lineage-aware `verify-public-disclosure` | adapt | current CHX-059–062, CHX-071, CHX-080; CHX-057; field `CHX-002`, `CHX-013`; release CHX-019, CHX-027–028 | one task-scoped hash-chained ledger for each new 0.4.1 run; revisions 3–5 record every discovered mechanism first under a stable finding id and require transactional promotion/merge/exclusion before close; revisions 4/5 freeze the complete digest-bound transitive predecessor lineage so numbering and relations survive empty hops; project-local revision-5 resolution uses prior full-tree reconnaissance, one reusable tactical repair per issue, and latest per-ledger integration; any globally installed repair uses the direct copy-on-write cross-ledger global integration without tactical precursors; late project-local issues require superseding complete resolved-issue coverage; publication disclosure preserves exact per-run issue ownership and accepts an unresolved immutable predecessor only through exactly one strictly later publication-resolved `supersedes` successor; deterministic reports and first-close/later-status projections agree; v1/v2/v3/v4 remain byte-exact readable and appendable; current worker starts may add an exact card/file binding, and ingestion projects genuine findings from a closed matching ledger into a nontruth observation inbox while silent-zero ledgers create nothing; future worker cards bind exact current schema-2 runtime identity and reject older-global drift; terminal historical reads may use only the validated host archive, never worker startup or writes; project-bound default at `PROJECT/chx-ledgers/`, external projectless fallback, causal-only entries, silent zero, no project-authority/truth effect, and no backfill or redo |

## Three-plane invariant

Every V5 task card binds all three planes without embedding their full contents in the prompt:

1. control: compact prompt, bounded control-only follow-ups, exact final handoff;
2. mathematical state: one frozen bounded snapshot and exact read/write capabilities;
3. narrative: bounded rationale, summary, intuition, limitations, and open boundary.

The task card remains immutable capability data. Removing it, replacing it with prompt prose, or
allowing a worker to infer capabilities from filesystem visibility is not a permitted simplification.

## Legacy main-path connections currently dormant

These mechanisms remain historical schema concepts but are not connected to
automatic V5 planning. Finding historical bytes does not authorize activation:

- default repair-lineage collapse to actionable leaves and the corresponding
  V4-only `--no-collapse-repairs` control; current V5 uses exact COW completion
  projection on the ordinary frontier;
- automatic attachment of the V4 `execution_profile` and
  `profile_obligations`; V5 keeps `adoption-plan` as standalone advice and uses
  the current assurance contract instead;
- automatic panel, Pulse, Paper/Audit, novelty, campaign-expansion, and expert-
  synthesis activation derived from that execution profile.

These are decision candidates, not missing Fact gates. V1-V4 authority import,
automatic project-background generation, automatic Learner/Grill activation,
and universal profile closure are intentionally retired safety boundaries and
must not be proposed as ordinary re-enablement.

## Prospectively restored context connections

The user explicitly approved L1 and L2 restoration for new work only. A
promoted Blackboard query may seed one exact new V5 task after its origin node,
origin snapshot, query, and hashes all validate; unrelated Research must be
planned separately, preserving the one-snapshot-per-round invariant. L2 accepts
only exact `prove`, `refute`, `compute`, `literature`, or `interpret` enum
values. An explicit `--mode` always wins; free text is rejected visibly; an
automatic hint applies only when its assurance, adverse binding, and later
program-math review signature equals the kind-derived default. The dedicated
Host role remains dispatch-only. Neither restoration backfills, reorders, or
invalidates a historical task.

The later user-approved Campaign connection is deliberately narrower than V4:
an exact Campaign id filters the V5 frontier and freezes its bounded nontruth
envelope. Version 0.9.3 makes creation direct and atomic through
`memory-add --campaign`; it is never inferred from `ACTIVE`. Untagged prior
Research stays untagged, mixed Campaign ids fail before writes, the four-factor
score still orders the scoped set, and no Campaign event creates truth. The
former Brave Future connection is retired without a compatibility alias;
ordinary frontier semantics preserve the useful recovery information.

## Release check

Before candidate freeze, enumerate the actual public commands and compare them with this inventory.
Every missing command needs an explicit mapped replacement or a user-approved removal. Run the
applicable inherited 0.3.6 regressions in addition to V5 canaries; a smaller suite is insufficient.
