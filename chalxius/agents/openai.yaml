interface:
  display_name: "Chalxius"
  short_description: "Research, audit, reader export, and opt-in learning"
  default_prompt: "Use $chalxius in automatic mode with the Chalxius research engine and one invariant Fact Graph admission contract. Render the fixed offline reader page only when I explicitly request visualization, and activate Chalxius Learner only when I explicitly request academic teaching or testing."
policy:
  allow_implicit_invocation: true
