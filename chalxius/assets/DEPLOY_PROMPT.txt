Verify the Chalxius candidate as one self-contained chalxius skill. Read
SKILL.md completely, verify
INHERITANCE.lock.json, run scripts/self_test.py and the complete unit suite, and
choose a writable PROJECT outside the skill directory. Do not start the legacy
Danus, Chalk, Grill Me, or coordinator runtimes; Chalk V4 is the sole embedded
research kernel. Do not install, package, migrate, or cut over without separate
explicit authorization.

Initialize or explicitly mode-init the project, then bind one of fast, auto, or
deep. Treat reasoning_mode as separate from plan-round assignment mode. Every
task card must bind the append-only mode event, deterministic execution_profile,
policy hash, and the same Fact-admission contract hash.

Follow execution_profile feature statuses exactly. Deep requires every
applicable high-cost feature. Auto requires each deterministic workload-triggered
feature. Fast leaves high-cost exploration available rather than automatic.
When parallel_clean_context_panel is required, use all genuinely distinct
currently callable clean-context slots. When barriered_blackboard_pulse is
required, persist a real plan before returns, ingest Wave 1, freeze the barrier
snapshot, record host dispatch after each native Wave-2 spawn and before its
return, ingest meaningful cross-review edges, and derive closure or explicit
whole-pulse abort. Available is opt-in; not_applicable must not be manufactured.
Score, cost, budget, duration, and burden are priority/load ordering only.

Every new unified round must freeze profile_obligations. After exact canonical
ingestion, run profile-closure-status. If features are required, create exactly
one typed entry for every required feature and no extras, then run
profile-closure-record before any single-Fact or atomic-bundle verifier task.
The receipt must bind each exact assignment task card, return, ingestion,
outcome, and effect. A no-required-feature Fast round is recomputably
not_required and must not receive a ceremonial receipt. A historical unified
round lacking profile_obligations must be replanned.

Keep evidence levels honest: native validation may be machine_verified, while
host capacity, specialist identity, campaign scope, and synthesis subject scope
are procedural_host_attestation. Use mixed_procedural_and_machine_verified for
their composites. Novelty and campaign events must be no earlier than the
governed round. Reused Paper snapshots must remain current and non-superseded;
an Audit whose Logic base was superseded is stale. Closure is
workflow_readiness_only, outside the invariant Fact-admission contract, and
cannot change its hash or strength. Accepted retries and audit must revalidate
the closure bytes.

Truth and adoption gates never vary by mode. Missing replay, source,
convention, quantifier, atomic-bundle, or fresh-verifier evidence leaves the
claim candidate_only_until_gate_satisfied or explicitly blocked. Use
preflight-return for mutable drafts, exact worker-final SHA-256 for ingestion,
and a packet-only different fresh verifier. Keep all Paper, Audit, Blackboard,
learning, draft, computation, and narrative objects outside Fact premises until
the invariant admission path succeeds.

For paper-led work, preserve exact source, researcher reconstruction, Paper
Audit, Blackboard exploration, Fact, and Learning planes. Require distinct
fresh source_fidelity and graph_structure reviewers before a logic snapshot;
require distinct fresh target_binding and audit_reasoning reviewers before an
Audit snapshot. Challenge and supersede errors append-only. A governed
full-fidelity Blackboard mirror is a projection, never Paper authority or a
Fact. Philosophy audits must distinguish local repair from literal refutation
and exact logical effect from broader dialectical effect.

Observe only the host-visible active-interval union. Exactly 1200 seconds does
not trigger; the first observation strictly greater than 1200 seconds creates
one complete notice while managed work remains runnable. Never send SIGKILL.
Keep one host_task_scope_id across one host task. Treat artifact, graph, event,
checkpoint, governance, and pulse caps as safety/integrity limits.

The integrated learning plane is publicly named Chalxius Learner. Activate it
only for explicit academic teaching/testing intent; ordinary research does not
start it. It may mount frozen Fact, pls-*, and bbs-* snapshots only when useful
to that learning request and may write only separately user-authorized
mastery/pedagogy evidence. It invokes no standalone Grill Me runtime, receives
no truth from non-Fact anchors, and never writes back. If the optional Grill Me
0.3.2-code companion is present, distinguish it as Grill Me Code and verify that
it is globally injected but starts only for explicit programming Grill or
Socratic intent, exposes no graph adapter, and has no research authority.

Treat V1-V3 projects as read-only and upgrade only an isolated copy. The
Python-library reasoning_mode=None path is inherited-Chalk fixture
compatibility only; official writable CLI initialization defaults to auto.

For an explicitly requested reader visualization, complete Reader Finalize as
the strengthened validation stage of the existing export-reader-html gate.
Prepare strict English reader-packet schema v1 from read-only frozen sources,
preserve native plane/status and exact text hashes, and require nonempty
summary, intuition, importance, and reasoning fields for every included node.
Verify the revision-11 contract: the fixed project output is byte-deterministic,
self-contained, offline, and has truth_effect none; all eligible real nodes and
edges remain present; All targets and All cards change card sizes rather than
topology; any card can be sized independently; double-click maximizes its
complete directed upstream/downstream path; sizing-only undo/redo is bounded to
100 session deltas; the Faceted/Plaques appearance choice is session-only; the
full minus and compact plus share the same normalized internal anchor at 29% of
rendered width and 50% of rendered height, with no screen-coordinate jump; the
size button is click-, tap-, and
keyboard-only while either card body remains directly draggable; every resized
card receives deterministic two-axis pivot compensation, an existing pin is
updated when present, and sizing never creates a pin; minimum zoom
keeps the internal control usable; selection glow follows the role silhouette;
semantic edges show enlarged category-colored direction markers at midpoint and
target while synthetic grouping links remain arrowless;
the Plaques inner ornament is dimmer than its outer semantic stroke; and
paired role-specific text widths and margins keep the control-plus-label
envelope within 3% of card center and multiline real-card labels are
left-justified without colliding with the control or silhouette; hovering the
card body or internal size control produces the same continuous related-edge
emphasis without a boundary flicker.
Verify deterministic `reader_finalize` metadata in both
embedded build metadata and stdout, bound to the source snapshot and packet
hash with presentation-readiness-only scope and no truth effect. The bilingual
Reload graph control must call same-document navigation only. Do not create a
watcher, polling or fetch loop, persistent visualization history, sidecar,
local-storage state, PDF workflow, learner record, or graph writeback.

Run project audit and mode-status before reporting. State the current mode event,
required-feature closure, Paper/Audit/Blackboard/Fact/Learning boundaries,
candidate versus admitted claims, computation replay status, warnings, and
residual uncertainty.
